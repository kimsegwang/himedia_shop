<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.tobi.himedia_shop.mapper.ProductBuyMapper">


    <insert id="buyProduct" parameterType="com.example.tobi.himedia_shop.model.PurchaseHistory">
        INSERT INTO purchasehistory (product_id, user_id, price, purchase_volume)
        VALUES (#{productId}, #{userId}, #{price}, #{purchaseVolume});
    </insert>
    <update id="updateProduct" parameterType="com.example.tobi.himedia_shop.dto.product.buy.StockUpdateDTO">
        UPDATE products SET stock = #{stock} WHERE id = #{productId}
    </update>
</mapper>

